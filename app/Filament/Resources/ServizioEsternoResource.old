<?php

declare(strict_types=1);

namespace Modules\IndennitaCondizioniLavoro\Filament\Resources;

use Filament\Forms\Form;
// use Modules\IndennitaCondizioniLavoro\Filament\Resources\ServizioEsternoResource\RelationManagers;
use Filament\Resources\Resource;
// use Filament\Forms;
use Filament\Tables\Table;
use Filament\Tables;
use Modules\IndennitaCondizioniLavoro\Filament\Resources\ServizioEsternoResource\Pages;
use Modules\IndennitaCondizioniLavoro\Models\ServizioEsterno;


// use Illuminate\Database\Eloquent\Builder;
// use Illuminate\Database\Eloquent\SoftDeletingScope;

class ServizioEsternoResource extends Resource {

    protected static ?string $model = ServizioEsterno::class;

    protected static ?string $navigationIcon = 'heroicon-o-rectangle-stack';

    public static function getFormSchema(): array
{
    return [
            ]);
    }

    public static function table(Table $table): Table {
        return $table
            ->columns([
            ])
            ->filters([
            ])
            ->actions([
                Tables\Actions\EditAction::make(),
            ])
            ->bulkActions([
                Tables\Actions\DeleteBulkAction::make(),
            ]);
    }

    public static function getRelations(): array {
        return [
        ];
    }

    public static function getPages(): array {
        return [
            'index' => Pages\ListServizioEsternos::route('/'),
            'create' => Pages\CreateServizioEsterno::route('/create'),
            'edit' => Pages\EditServizioEsterno::route('/{record}/edit'),
        ];
    }
}
