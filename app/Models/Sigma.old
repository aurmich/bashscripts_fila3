<?php

declare(strict_types=1);

namespace Modules\Sigma\Models;

use Illuminate\Support\Carbon;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\Model;
// use Laravel\Scout\Searchable;
use Modules\Xot\Traits\Updater;

/**
 * Modules\Sigma\Models\Sigma.
 *
 * @property int $id
 * @property string|null $db
 * @property string|null $tbl
 * @property string|null $descr
 * @property int|null $n_rows
 * @property string|null $datemod
 * @property Carbon|null $created_at
 * @property Carbon|null $updated_at
 * @property string|null $updated_by
 * @method static Builder|Sigma newModelQuery()
 * @method static Builder|Sigma newQuery()
 * @method static Builder|Sigma query()
 * @method static Builder|Sigma whereCreatedAt($value)
 * @method static Builder|Sigma whereDatemod($value)
 * @method static Builder|Sigma whereDb($value)
 * @method static Builder|Sigma whereDescr($value)
 * @method static Builder|Sigma whereId($value)
 * @method static Builder|Sigma whereNRows($value)
 * @method static Builder|Sigma whereTbl($value)
 * @method static Builder|Sigma whereUpdatedAt($value)
 * @method static Builder|Sigma whereUpdatedBy($value)
 * @mixin \Eloquent
 */
class Sigma extends Model {
    use Updater;
    protected $fillable = ['id', 'db', 'tbl', 'descr', 'n_rows', 'datemod', 'created_at', 'updated_at', 'updated_by'];
    
    // use Searchable;

    protected $connection = 'menu';
     // this will use the specified database connection
    protected $table = 'lista_tbl';
    
    public $timestamps = true;
}
