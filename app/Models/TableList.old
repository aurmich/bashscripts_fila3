<?php

declare(strict_types=1);

namespace Modules\Sigma\Models;

use Illuminate\Support\Carbon;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\Model;
// use Laravel\Scout\Searchable;
use Modules\Xot\Traits\Updater;

/**
 * Modules\Sigma\Models\TableList.
 *
 * @property int $id
 * @property string|null $db
 * @property string|null $tbl
 * @property string|null $descr
 * @property int|null $n_rows
 * @property string|null $datemod
 * @property Carbon|null $created_at
 * @property Carbon|null $updated_at
 * @property string|null $updated_by
 * @method static Builder|TableList newModelQuery()
 * @method static Builder|TableList newQuery()
 * @method static Builder|TableList query()
 * @method static Builder|TableList whereCreatedAt($value)
 * @method static Builder|TableList whereDatemod($value)
 * @method static Builder|TableList whereDb($value)
 * @method static Builder|TableList whereDescr($value)
 * @method static Builder|TableList whereId($value)
 * @method static Builder|TableList whereNRows($value)
 * @method static Builder|TableList whereTbl($value)
 * @method static Builder|TableList whereUpdatedAt($value)
 * @method static Builder|TableList whereUpdatedBy($value)
 * @mixin \Eloquent
 */
class TableList extends Model {
    use Updater;
    protected $fillable = ['id', 'db', 'tbl', 'descr', 'n_rows', 'datemod', 'created_at', 'updated_at', 'updated_by'];
    
    // use Searchable;

    protected $connection = 'menu';
     // this will use the specified database connection
    protected $table = 'lista_tbl';
    
    public $timestamps = true;

    // end search
    // --------------------------
}
